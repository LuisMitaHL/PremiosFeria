# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/dist

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# docker
.docker

# prod stack (bind-mounted DB data + secrets — never commit)
data/
.env.prod
# operator-supplied seed CSVs (passwords!) — only the .gitkeep is committed
seed/*.csv

# local dev instance (generated by dev.sh)
.dev
# shipped mock auth service (not generated by dev.sh)
!.dev/auth-mock.mjs
